import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/Main/components/body.dart';

//Root of the MainScreen
class MainScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: MyApp(),
    );
  }
}
