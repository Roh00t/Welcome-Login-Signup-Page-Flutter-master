import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/Timer/components/main.dart';

//Root of the TimerScreen
class TimerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
